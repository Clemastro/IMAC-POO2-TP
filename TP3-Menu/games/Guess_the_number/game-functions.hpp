#pragma once
#include <iostream> //use inflow/outflow stream
#include <random>   //use random number

// random Int return a random integer between (min) & (max)
int randomInt(int min, int max);

// get a number from a user
int get_int_from_user();

// launch the game
void play_guess_the_number();